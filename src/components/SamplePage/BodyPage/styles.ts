import { styled } from '@mui/material';

export const CustomTitleText = styled('span')`
  font-size: 5.5rem;
  font-weight: bold;
  margin-left: 1rem;
  color: ${({ className }) => className};
`;
export const CustomSubTitleText = styled('span')`
  font-size: 1.8rem;
  font-weight: bold;
  margin-left: 1rem;
  color: ${({ className }) => className};
`;
export const CustomDestakSubTitleText = styled('span')`
  font-size: 1.8rem;
  font-weight: bold;
  color: ${({ className }) => className};
`;
